const utils = require('../utils');
const {App} = require("../../utils/server");

describe('load-fixtures', function () {

    beforeEach(function (done) {
        utils.beforeEach(done);
    });

    describe('load-fixtures', function () {
        it('load-fixtures', function (done) {
            // fake test for loading fixtures
            let app = App;
            done();
        });
    });
});